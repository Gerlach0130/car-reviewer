<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Car Reviewer</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header>
          {{#if logged_in}}
          <button><a href="/profile">Profile</a></button>
          <button><a href="#" id="logout">Logout</a></button>
          {{else}}
          <button><a href="/login" class="login-button">Login</a></button>
          <button><a href="/signup" class="signup-button">Sign Up</a></button>
          {{/if}}
  </header>
  {{{ body }}}
  <footer>
    
  </footer>
  <script src="/js/logout.js"></script>
  {{#if logged_in}}
  {{/if}}
</body>

</html>